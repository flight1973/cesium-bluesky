# Cesium-BlueSky Python dependencies
# Pin to compatible ranges; exact pins in a lockfile if needed.

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
pydantic-settings>=2.0

# HTTP client (async — for AWC, FAA, EUROCONTROL fetches)
httpx>=0.27

# Fast JSON serialization
orjson>=3.9

# Geodesy
pyproj>=3.6

# Graph database driver
neo4j>=5.20

# Credential encryption
cryptography>=42

# BlueSky simulator dependencies
numpy>=1.26
scipy>=1.12
matplotlib>=3.8
msgpack>=1.0

# Docs site rendering
markdown>=3.5
pygments>=2.17

# BlueSky optional (spatial filtering)
rtree>=1.2

# WebSocket support (via uvicorn[standard])
# websockets is pulled in by uvicorn[standard]
